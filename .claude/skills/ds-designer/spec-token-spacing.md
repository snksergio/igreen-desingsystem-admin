---
name: spec-token-spacing
description: >
  ALIAS pra spec-token (tipo=spacing). Mantido pra retrocompatibilidade.
  Conteúdo consolidado em .claude/skills/ds-designer/spec-token.md.
---

# DS Designer — Spacing (alias)

Este arquivo foi consolidado em `spec-token.md`. Use:

```
Skill: ds-designer/spec-token
Args:  tipo=spacing
```

A skill consolidada inclui:
- Verificação prévia obrigatória (regra comum)
- Spec com perspectiva Strategist (alternativas + assumption)
- Seção `spacing`: grupos `gap`/`space`/`pad`, escala base 4px×n, tokens de componente (`padCard`/`padPage`), guia "qual grupo usar"
- Doc de referência: `.ai/context/tokens/spacing.md`

---
name: spec-token-color
description: >
  ALIAS pra spec-token (tipo=color). Mantido pra retrocompatibilidade.
  Conteúdo consolidado em .claude/skills/ds-designer/spec-token.md.
---

# DS Designer — Cor semântica (alias)

Este arquivo foi consolidado em `spec-token.md`. Use:

```
Skill: ds-designer/spec-token
Args:  tipo=color
```

A skill consolidada inclui:
- Verificação prévia obrigatória (regra comum)
- Spec com perspectiva Strategist (alternativas + assumption)
- Seção `color`: roles (bg/fg/border/ring/overlay), sufixos (`-inverted`, `on-*`), variantes obrigatórias (subtle/muted/on-*), fluxo de criação, nomes proibidos
- Doc de referência: `.ai/context/tokens/color.md`
